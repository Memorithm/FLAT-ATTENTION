[package]
name = "flat-epg-reference"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
description = "Correctness-first Elastic Positional Geometry reference contract for FLAT-ATTENTION"
license-file = "../../LICENSE.md"

[dependencies]
epg-core = { path = "../epg-core" }
flat-attention = { path = "../.." }
flat-semantic = { path = "../flat-semantic" }
