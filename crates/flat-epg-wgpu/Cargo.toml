[package]
name = "flat-epg-wgpu"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
description = "Correctness-first WGPU qualification kernels for Elastic Positional Geometry"
license-file = "../../LICENSE.md"

[dependencies]
epg-core = { path = "../epg-core" }
flat-attention = { path = "../..", features = ["wgpu"] }
wgpu = "0.20"
pollster = "0.3"

[dev-dependencies]
flat-epg-reference = { path = "../flat-epg-reference" }
naga = { version = "30.0", features = ["wgsl-in"] }
