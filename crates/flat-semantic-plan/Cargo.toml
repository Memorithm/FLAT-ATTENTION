[package]
name = "flat-semantic-plan"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
license-file = "../../LICENSE.md"
description = "Device-aware admissibility planning for selected FLAT attention semantics"

[dependencies]
flat-attention = { path = "../.." }
flat-semantic = { path = "../flat-semantic" }
flat-semantic-control = { path = "../flat-semantic-control" }
flat-semantic-execution = { path = "../flat-semantic-execution" }
flat-semantic-selection = { path = "../flat-semantic-selection" }

[dev-dependencies]
flat-semantic-registry = { path = "../flat-semantic-registry" }
