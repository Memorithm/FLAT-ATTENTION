[package]
name = "flat-semantic-registry"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
description = "Deterministic semantic identity registry for FLAT-ATTENTION"
license-file = "../../LICENSE.md"

[dependencies]
flat-semantic = { path = "../flat-semantic" }
