[package]
name = "flat-wgpu-router"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
publish = false
description = "Prepared live WGPU kernel routing for qualified FLAT-ATTENTION realizations"
license-file = "../../LICENSE.md"

[dependencies]
flat-attention = { path = "../..", features = ["wgpu"] }
